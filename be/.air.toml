root = "."
tmp_dir = "tmp"

[build]
  # Windows only executes binaries with the .exe extension.
  cmd = "go build -o ./tmp/api.exe ./cmd/api"
  entrypoint = "./tmp/api.exe"
  delay = 1000
  exclude_dir = ["tmp"]
  include_ext = ["go", "toml"]
  stop_on_error = true

[log]
  time = true
