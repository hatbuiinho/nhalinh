root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/api ./cmd/api"
  entrypoint = "./tmp/api"
  delay = 1000
  exclude_dir = ["tmp"]
  include_ext = ["go", "toml"]
  stop_on_error = true

[log]
  time = true
